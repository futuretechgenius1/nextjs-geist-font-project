version: "3.8"

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - JWT_SECRET=your_jwt_secret_here
    volumes:
      - ./backend/data.db:/app/backend/data.db
    restart: unless-stopped
